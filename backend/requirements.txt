# Core runtime
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
alembic>=1.13
pydantic>=2.7
pydantic-settings>=2.5
email-validator>=2.0

# Auth / security
pwdlib[argon2]>=0.2
PyJWT>=2.8

# Postgres driver (code uses postgresql+psycopg://)
psycopg[binary]>=3.1

# Env loading (used transitively by pydantic-settings)
python-dotenv>=1.0

# Dev / test
pytest>=8.0
httpx>=0.27
